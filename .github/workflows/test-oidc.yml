name: Test AWS OIDC

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  test-aws-connection:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          # TODO: <AWS_ACCOUNT_ID> durch die echte Account-ID ersetzen,
          # sobald terraform/bootstrap für diesen Account gelaufen ist.
          role-to-assume: arn:aws:iam::<AWS_ACCOUNT_ID>:role/github-oidc-test
          aws-region: eu-central-1

      - name: Show AWS identity
        run: aws sts get-caller-identity
